import BaseApply, {
  ApplyElementProps,
} from '@/components/Designer/components/Element/Base';

const ApplyElement = (props: ApplyElementProps) => <BaseApply {...props} />;

export default ApplyElement;
