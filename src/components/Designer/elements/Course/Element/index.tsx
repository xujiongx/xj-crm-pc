import BaseApply, {
  ApplyElementProps,
} from '@/components/Designer/components/Element/Base';

const CourseApplyElement = (props: ApplyElementProps) => (
  <BaseApply {...props} />
);

export default CourseApplyElement;
