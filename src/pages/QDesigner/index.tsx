import QDesigner from '@/components/QDesigner';

const Page: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '800px',
        background: '#ccc',
      }}
    >
      <QDesigner />
    </div>
  );
};

export default Page;
